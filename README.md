Set the `FLASK_APP` and `FLASK_ENV` environment variables, in Windows:
```
set FLASK_APP=fariasweb
set FLASK_ENV=development
```
In bash:
```
export FLASK_APP=fariasweb
export FLAS_ENV=development
```
